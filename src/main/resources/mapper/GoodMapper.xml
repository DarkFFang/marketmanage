<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fang.marketmanage.dao.GoodMapper">
    <resultMap id="goodResultMap" type="com.fang.marketmanage.entity.Good">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="typeId" column="typeid"/>
        <result property="sDate" column="sdate"/>
        <result property="eDate" column="edate"/>
        <result property="price" column="price"/>
        <result property="unitId" column="unitid"/>
        <result property="other" column="other"/>
    </resultMap>
    <insert id="addNewGood" parameterType="com.fang.marketmanage.entity.Good">
        INSERT INTO good VALUES (#{name},#{typeId},#{sDate},#{eDate},#{price},#{unitId},#{other})
    </insert>
    <select id="findGoodList" resultMap="goodResultMap">
        SELECT * FROM good
    </select>
    <delete id="deleteGoodById">
        DELETE FROM good where id=#{id}
    </delete>
    <update id="updateGoodById">
        UPDATE good SET name=#{name},typeid=#{typeId},sdate=#{sDate},edate=#{eDate},price=#{price},unitId=#{unitId},other=#{other}
        WHERE id=#{id}
    </update>
    <select id="findGoodByName" resultMap="goodResultMap">
        SELECT * FROM good WHERE name=#{name}
    </select>
    <select id="findGoodListByType" resultMap="goodResultMap">
        SELECT * FROM good WHERE typeid=(SELECT id FROM goodtype WHERE name=#{name})
    </select>
</mapper>